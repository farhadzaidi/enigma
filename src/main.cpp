#include <vector>
#include <string>
#include <cstdlib>

#include "uci.hpp"

int main(int argc, char* argv[]) {
    // std::vector<std::string> args;
    // for (int i = 1; i < argc; i++) {
    //     args.push_back(argv[i]);
    // }

    // if (args.size() == 0) {
    //     uci_loop();
    // } else {
    //     if (args[0] == "perft") {
    //         // Perft
    //     }
    // }

    
}
